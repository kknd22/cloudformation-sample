---

- name: install php and its modules
  yum: 
    name: {{ item }}
    state: latest
  with_items:
    - php
    - php-mysql
    - php-gd

- name: start httpd
  service:
    name: httpd
    state: restarted
    enabled: yes
  become: yes
  become_user: root
